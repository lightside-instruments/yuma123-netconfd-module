# Overview

YANG module for management of gcode execution

Allows running gcode command and waiting for its completion



# Dependencies
Installed netconfd and yangcli
 apt-get install netconfd yangcli

# Installation
 autoreconf -i -f
 ./configure CFLAGS="-g -O0"  CXXFLAGS="-g -O0" --prefix=/usr
 make
 make install


# Testing installation

 netconfd --module=lsi-lights  --no-startup --superuser=pi

Other terminal:
 yangcli --server=localhost --user=pi --password=raspberry



...

 yangcli pi@localhost> create /gcode -- cmd="G0 X0Y0Z4"

 yangcli pi@localhost> commit
 
...

And the CNC machine moves.
