# Overview
Simple template module for netconfd - with config true instrumentation.

Replace contact information with yours, lsi -> your organization, bar -> your module base name


# Dependencies
Installed netconfd and yangcli
 apt-get install netconfd yangcli

# Installation
 autoreconf -i -f
 ./configure CFLAGS="-g -O0"  CXXFLAGS="-g -O0" --prefix=/usr
 make
 make install

# Testing installation

 netconfd --module=lsi-bar  --no-startup --superuser=$USER

Other terminal:
 yangcli --server=localhost --user=$USER


yangcli vladimir@localhost> create /bar/enabled

Filling optional leaf /bar/enabled:
Enter boolean value for leaf <enabled>
yangcli vladimir@localhost:create> true

RPC OK Reply 2 for session 1:

yangcli vladimir@localhost> commit


This will create a file in the directory you started netconfd containing "1".
