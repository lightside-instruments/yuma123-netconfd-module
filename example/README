The following script is provided as practical example. It displays image generated as /tmp/image.jpg in the script.

Start server with the module loaded:

netconfd --module=lsi-display --superuser=pi --no-startup

Edit networks.xml adding the NETCONF access parameters for your device.

Usage:

python3 test.py --config=networks.xml
